# ============================================================================
# LLM-based Network Anomaly Detection (6G-AIOps)
# Requirements File
# ============================================================================
# 
# This file lists all Python dependencies needed for the project.
#
# Installation:
#   pip install -r requirements.txt
#
# Note: Install PyTorch separately with CUDA support if you have a GPU:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# ============================================================================

# Core Deep Learning Framework
torch>=2.0.0                    # PyTorch (install with CUDA separately)
torchvision>=0.15.0
torchaudio>=2.0.0

# HuggingFace Ecosystem
transformers>=4.35.0            # For phi-2 and tokenizers
datasets>=2.14.0                # Dataset utilities
accelerate>=0.24.0              # Distributed training support
peft>=0.6.0                     # Parameter-Efficient Fine-Tuning (LoRA)
trl>=0.7.0                      # Transformer Reinforcement Learning

# Quantization
bitsandbytes>=0.41.0            # 4-bit quantization (requires CUDA)
scipy>=1.10.0                   # Required by bitsandbytes

# Data Processing
pandas>=2.0.0                   # Data manipulation
numpy>=1.24.0                   # Numerical computations
scikit-learn>=1.3.0             # ML metrics and utilities

# Visualization
matplotlib>=3.7.0               # Plotting
seaborn>=0.12.0                 # Statistical visualizations

# Progress Tracking
tqdm>=4.65.0                    # Progress bars

# Utilities
jupyter>=1.0.0                  # Jupyter notebooks (optional)
ipython>=8.12.0                 # Interactive Python (optional)

# Monitoring (Optional but Recommended)
tensorboard>=2.13.0             # Training visualization
wandb>=0.15.0                   # Weights & Biases (optional, for advanced logging)

# Development Tools (Optional)
black>=23.0.0                   # Code formatting
flake8>=6.0.0                   # Linting
pytest>=7.4.0                   # Testing

# ============================================================================
# INSTALLATION NOTES:
# ============================================================================
#
# 1. For GPU Support (Recommended):
#    First install PyTorch with CUDA:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#    
#    Then install other requirements:
#      pip install -r requirements.txt
#
# 2. For CPU Only (Slower):
#    pip install -r requirements.txt
#
# 3. Verify Installation:
#    python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA Available: {torch.cuda.is_available()}')"
#    python -c "import transformers; print(f'Transformers: {transformers.__version__}')"
#
# 4. Troubleshooting:
#    - If bitsandbytes fails: Requires CUDA-capable GPU
#    - If out of memory: Reduce batch size in training config
#    - If slow: Make sure CUDA is properly installed
#
